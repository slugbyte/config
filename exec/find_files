#!/usr/bin/env bash
# this is uses by nvim telescope plugin as the find_files_command
rg --files --follow --hidden --no-ignore --max-filesize 30K  \
| rg -v node_modules \
| rg -v target \
| rg -v .shadow-cljs \
| rg -v .clj-kondo \
| rg -v '\.git' \
| rg -v '\.lsp' \
| rg -v 'runtime'
