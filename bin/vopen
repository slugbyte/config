#!/bin/bash
# vopen [branch]

if [[ -d .jj ]]; then
    branch="${1:-main}"
    git -C "$(jj git root)" open origin "$branch"
    exit 0
fi

if [[ -n "$1" ]]; then
    git open origin "$1"
else
    git open
fi
