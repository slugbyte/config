#!/usr/bin/env fish
# vopen [branch]

if test -d .jj
    set branch main
    if test -n "$argv[1]"
        set branch "$argv[1]"
    end
    git -C $(jj git root) open origin $branch
    return
end
if test -n "$argv[1]"
    git open origin "$argv[1]"
else
    git open
end
