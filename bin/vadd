#!/bin/bash

if [[ -d ./.jj ]]; then
    echo "ERROR: git_add not supported in jj repositories"
    exit 1
fi

# If no arguments, add everything
if [[ $# -eq 0 ]]; then
    git add .
else
    # Add specified files
    git add "$@"
fi
